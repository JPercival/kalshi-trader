<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login — Kalshi Paper Trader</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; background: #0f1117; color: #e0e0e0; display: flex; justify-content: center; align-items: center; min-height: 100vh; }
    .login-card { background: #1a1d27; border-radius: 12px; padding: 3rem; border: 1px solid #2a2d37; text-align: center; max-width: 400px; width: 90%; }
    .login-card h1 { color: #4f9cf7; margin-bottom: 0.5rem; font-size: 1.5rem; }
    .login-card p { color: #8b8fa3; margin-bottom: 2rem; }
    .login-btn { display: inline-block; background: #4285f4; color: white; padding: 0.75rem 2rem; border-radius: 6px; text-decoration: none; font-weight: 600; font-size: 1rem; transition: background 0.2s; }
    .login-btn:hover { background: #3367d6; }
    .error { background: #3a1a1a; color: #f87171; padding: 0.75rem; border-radius: 6px; margin-bottom: 1.5rem; font-size: 0.9rem; }
  </style>
</head>
<body>
  <div class="login-card">
    <h1>📊 Kalshi Paper Trader</h1>
    <p>Sign in to access the dashboard</p>
    <% if (error) { %>
      <div class="error"><%= error === 'auth_failed' ? 'Authentication failed. Your email may not be authorized.' : error %></div>
    <% } %>
    <a href="/auth/google" class="login-btn">Sign in with Google</a>
  </div>
</body>
</html>
